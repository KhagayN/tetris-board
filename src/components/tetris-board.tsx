import * as React from 'react'

// Define props for TetrisBoard component
type TetrisBoardProps = {
    fields: any[],
    gameOver: boolean,
    score: number,
    level: number,
    rotate: number
}

//
